/**
 * Agents Page — /agents
 *
 * Full-page wrapper for the AgentCommandWorkspace.
 * Manager-gated. Accessible from the sidebar under manager navigation.
 */

import { AgentCommandWorkspace } from '@/components/AgentCommandWorkspace';

export default function AgentsPage() {
  return <AgentCommandWorkspace />;
}
